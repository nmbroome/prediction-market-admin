export type Markets = {
    marketName: string;
    description: string;
    tokenPool: string;
    marketMaker: string;
};

export type Predictions = {
    marketName: string;
    tokenAmount: string;
    outcomeId: string;
};